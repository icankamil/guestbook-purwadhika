#Guestbook
This is monorepo that consist of backend and frontend project. Install all dependencies and run the project from root directory, as for deployment there is CI pipeline using Github actions and Docker for containerization.

##Frontend
Nextjs app router, zod as validators, shadcn as component library

##Backend
Expressjs as backend, Prisma as ORM, zod as validation